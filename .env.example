# Campus Copilot BFF
# 默认本地监听端口
PORT=8787

# 正式 provider 路线
OPENAI_API_KEY=
GEMINI_API_KEY=

# 可选：覆盖 provider API base URL（调试代理或本地网关时使用）
OPENAI_BASE_URL=
GEMINI_BASE_URL=

# 仍未进入正式主路径，仅保留字段占位，不建议当前启用
GEMINI_ACCESS_TOKEN=
