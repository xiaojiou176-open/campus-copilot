# Current View Triage Example

Sample snapshot-based output:

> The current workspace says you should look at Canvas first.
>
> Why:
> - `Homework 1` is already present in the current view
> - recent updates are still light, so the focus queue is easy to review
> - there is not enough evidence here to invent other overdue work

Trust gaps:

- this example is snapshot-only, not live browser truth
- it only reflects what was exported into the current workspace snapshot
